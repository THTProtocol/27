{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "matches": {
      "$matchId": {
        ".validate": "newData.hasChildren(['id','status','playerA','playerB'])",
        "status": { ".validate": "newData.isString()" }
      }
    }
  }
}
