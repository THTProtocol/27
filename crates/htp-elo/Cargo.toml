[package]
name = "htp-elo"
version.workspace = true
edition.workspace = true
description = "HTP ELO Rating System — skill-based leaderboard rankings"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
rusqlite = { version = "0.31", features = ["bundled"] }
