[package]
name = "tournament-engine"
version = "0.1.0"
edition = "2021"
description = "HTP Tournament Engine — bracket management with spectator parimutuel betting"

[dependencies]
kdapp = { path = "../kdapp" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
